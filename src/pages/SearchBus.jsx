import React from "react";
import SearchBar from "../components/ui/search/SearchBar";

const SearchBus = () => {
  return (
    <div>
      <SearchBar />
    </div>
  );
};

export default SearchBus;
